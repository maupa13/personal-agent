name: personal-agent-rus
services:
  searxng:
    image: ${PA_SEARXNG_IMAGE:-searxng/searxng:2026.8.5-1689cb1b5}
    container_name: par-rus-searxng
    restart: unless-stopped
    environment:
      SEARXNG_SECRET: ${PA_SEARXNG_SECRET:?PA_SEARXNG_SECRET must be set in .env}
    volumes:
    - ./searxng/settings.yml:/etc/searxng/settings.yml:ro
  browser:
    build:
      context: ./services/browser
    image: ${PA_BROWSER_IMAGE:-personal-agent-browser:1.0.0}
    container_name: par-rus-browser
    restart: unless-stopped
    environment:
      PA_BROWSER_HOST: 0.0.0.0
      PA_BROWSER_PORT: 8000
    shm_size: 1gb
    healthcheck:
      test:
      - CMD
      - python
      - -c
      - import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health',
        timeout=3)
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 10s
  code-worker:
    image: ${PA_CODE_WORKER_IMAGE:-personal-agent-code-worker:1.0.0}
    container_name: par-rus-code-worker
    restart: unless-stopped
    network_mode: none
    read_only: true
    tmpfs:
    - /work:rw,nosuid,nodev,size=256m,mode=0711
    volumes:
    - par-rus-code-ipc:/ipc
    environment:
      PA_CODE_SOCKET: /ipc/code-worker.sock
      PA_CODE_WORK_ROOT: /work
      PA_CODE_MAX_SOURCE_BYTES: ${PA_CODE_MAX_SOURCE_BYTES:-262144}
      PA_CODE_MAX_OUTPUT_BYTES: ${PA_CODE_MAX_OUTPUT_BYTES:-1048576}
      PA_CODE_MAX_TIMEOUT_SECONDS: ${PA_CODE_MAX_TIMEOUT_SECONDS:-30}
      PA_CODE_SOCKET_GID: '10001'
    cap_drop:
    - ALL
    cap_add:
    - CHOWN
    - SETUID
    - SETGID
    - KILL
    security_opt:
    - no-new-privileges:true
    pids_limit: 128
    mem_limit: 1g
    cpus: 2.0
    healthcheck:
      test:
      - CMD
      - python3
      - -c
      - import os,socket; p='/ipc/code-worker.sock'; assert os.path.exists(p); s=socket.socket(socket.AF_UNIX,socket.SOCK_STREAM);
        s.settimeout(2); s.connect(p); s.close()
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 15s
    build:
      context: ./services/code-worker
  ollama:
    image: ${PA_OLLAMA_IMAGE:-ollama/ollama:0.32.6}
    container_name: par-rus-ollama
    restart: unless-stopped
    gpus: all
    environment:
      OLLAMA_KEEP_ALIVE: 15m
      OLLAMA_MAX_LOADED_MODELS: '1'
      OLLAMA_NUM_PARALLEL: '1'
      OLLAMA_CONTEXT_LENGTH: '32768'
    volumes:
    - par-rus-models:/root/.ollama
    healthcheck:
      test:
      - CMD
      - ollama
      - list
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 10s
  core:
    build:
      context: ./services/core
    image: ${PA_CORE_IMAGE:-personal-agent-core:1.0.0}
    container_name: par-rus-core
    restart: unless-stopped
    environment:
      PA_PRODUCT_FAMILY: Personal Agent
      PA_PRODUCT_NAME: Personal Agent Rus
      PA_EDITION: rus
      PA_LOCALE: ru-RU
      PA_VERSION: ${PA_VERSION:-1.0.0}
      PA_RUNTIME_PROFILE: ${PA_RUNTIME_PROFILE:-local}
      PA_HOST: 0.0.0.0
      PA_PORT: 8080
      PA_OLLAMA_URL: ${PA_OLLAMA_URL:-http://ollama:11434}
      PA_SEARXNG_URL: ${PA_SEARXNG_URL:-http://searxng:8080}
      PA_BROWSER_URL: ${PA_BROWSER_URL:-http://browser:8000}
      PA_BOOTSTRAP_MODEL: ${PA_BOOTSTRAP_MODEL:-qwen3:0.6b}
      PA_ADMIN_TOKEN: ${PA_ADMIN_TOKEN:?PA_ADMIN_TOKEN must be set in .env}
      PA_AUTH_MODE: ${PA_AUTH_MODE:-personal}
      PA_REGISTRATION_POLICY: ${PA_REGISTRATION_POLICY:-open}
      PA_SECURE_COOKIES: ${PA_SECURE_COOKIES:-0}
      PA_PUBLIC_URL: ${PA_PUBLIC_URL:-}
      PA_DATABASE_URL: ${PA_DATABASE_URL:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      PA_OPENAI_API_KEY: ${PA_OPENAI_API_KEY:-}
      PA_OPENAI_BASE_URL: ${PA_OPENAI_BASE_URL:-https://api.openai.com/v1}
      PA_OPENAI_PROVIDER_TYPE: ${PA_OPENAI_PROVIDER_TYPE:-openai_responses}
      PA_OPENAI_PROVIDER_ID: ${PA_OPENAI_PROVIDER_ID:-openai}
      PA_OPENAI_PROVIDER_NAME: ${PA_OPENAI_PROVIDER_NAME:-OpenAI}
      PA_VPN_ROUTING_ENABLED: ${PA_VPN_ROUTING_ENABLED:-0}
      PA_VPN_ROUTING_MODE: ${PA_VPN_ROUTING_MODE:-}
      PA_VPN_PREFERENCE_ID: ${PA_VPN_PREFERENCE_ID:-}
      PA_VPN_VPS2_HOST: ${PA_VPN_VPS2_HOST:-}
      PA_VPN_UPSTREAM_HOST: ${PA_VPN_UPSTREAM_HOST:-}
      PA_VPN_UPSTREAM_IP: ${PA_VPN_UPSTREAM_IP:-}
      PA_VPN_ALLOWED_IPS: ${PA_VPN_ALLOWED_IPS:-}
      PA_VPN_PROFILE_FILE: ${PA_VPN_PROFILE_FILE:-}
      PA_LAN_ENABLED: ${PA_LAN_ENABLED:-0}
      PA_LAN_PUBLIC_URL: ${PA_LAN_PUBLIC_URL:-}
      PA_SECRETS_DIR: /data/secrets
      PA_LOG_DIR: /data/logs
      PA_USER_TOUR_VERSION: 1
      PA_ADMIN_TOUR_VERSION: 1
      PA_DB: /data/personal-agent-rus.db
      PA_CODE_SOCKET: /run/personal-agent-code/code-worker.sock
    volumes:
    - par-rus-data:/data
    - par-rus-code-ipc:/run/personal-agent-code:ro
    ports:
    - ${PA_BIND_IP:-127.0.0.1}:${PA_UI_PORT:-3100}:8080
    depends_on:
      ollama:
        condition: service_healthy
      searxng:
        condition: service_started
      browser:
        condition: service_healthy
    healthcheck:
      test:
      - CMD
      - python
      - -c
      - import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/api/health',
        timeout=3)
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 10s
volumes:
  par-rus-data:
    name: par-rus-data
  par-rus-models:
    name: par-rus-models
  par-rus-code-ipc:
    name: par-rus-code-ipc
