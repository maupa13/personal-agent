# Personal Agent Rus v0.7.0 — Local Release Evidence

## Deterministic local status

PASS:

- signed package integrity after fresh ZIP extraction;
- static/package hygiene;
- Windows lifecycle command contract;
- Product Shell desktop/mobile browser journeys;
- Auth/registration + accounts CSRF contract;
- Providers/routing;
- Web/Research regression;
- Files/Artifacts regression;
- Code sandbox regression, including no `preexec_fn` threaded-subprocess pattern;
- Billing/Entitlements/payment adapter regression;
- durable Task/Step/Event engine;
- Research → verified MD/XLSX/PDF;
- VPS server-lite bundle;
- SSH host-key pinning and no persisted SSH credentials;
- Debian/Ubuntu root Docker/Compose bootstrap contract;
- staged deployment + rollback contract;
- internal and public HTTPS exact-version hot-verification contract;
- optional remote-provider bootstrap contract;
- lightweight Admin monitoring;
- Windows Private-LAN lifecycle contract.

## Environment-bound — not promoted to PASS

- real Linux VPS SSH/bootstrap/deploy;
- real DNS + trusted public HTTPS;
- actual external AI provider inference on deployed VPS and public browser chat;
- real VPS rollback/data persistence;
- physical phone/laptop LAN access;
- real Windows lifecycle/reboot/clean-machine acceptance;
- real YooKassa merchant checkout/webhook/recurring payment;
- real Docker Code worker gate on reference Windows.

This evidence bundle therefore proves the deterministic release package, not the external environment gates.
