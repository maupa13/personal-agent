# Providers

Provider and model identities are separate. Current adapters: local/admin Ollama and OpenAI-compatible. Search/browser are also provider-independent tools. Routing stores `provider_id + model_id`; USER sees only modes/capabilities.
