# Personal Agent Rus 0.8.0-alpha.4 — UX / Admin Hardening

## Scope

- theme: dark / light / system;
- UI/response language preferences;
- backend-authoritative local/remote execution policy;
- accuracy-first tone presets including ironic/meme;
- local OWNER loopback Admin access + hardened break-glass path;
- Admin provider/model/routing UX with OpenAI Responses provider type for VPS test profile;
- expiring read-only Share Chat;
- server-side feedback;
- collapsed sidebar / project / form / textarea / code-editor layout hardening;
- in-product Local Setup and Admin Guide.

## Security / privacy invariants

- share token is stored only as SHA-256 hash;
- share page is read-only and `noindex`;
- share link does not grant account/workspace access;
- feedback UI warns against secrets;
- remote API key remains server-side;
- local-owner Admin shortcut is loopback-only;
- break-glass token remains emergency-only;
- `local_only` execution policy is enforced by Core.

## Acceptance

Machine-readable: `tests/alpha4-registry.json`.
Authoritative suite: `tests/ux_admin_hardening_acceptance.py`, plus full Core API and Chromium user journeys.

## Not claimed

`PG-RUNTIME-001`, real YooKassa merchant, real VPS/HTTPS, physical second-device LAN, Windows reboot, clean-machine and `VPS-EGRESS-001` remain separate gates.
